<%= form_with(model: rating, local: true) do |form| %>
  <% if rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rating.errors.count, "error") %> prohibited this rating from being saved:</h2>

      <ul>
      <% rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= form.label :nota %>
    <%= form.number_field :nota, id: :rating_nota %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_area :comentario, id: :rating_comentario %>
  </div>
  
  <!-- #Dan -->
  <div class="field" style="visibility: hidden">
    <%= form.label :banda_id %>
    <%= form.text_field :banda_id, id: :rating_banda_id %>
  </div> 
  
  <!-- #Dan -->
  <div class="field" style="visibility: hidden">
    <%= form.label :fa_id %>
    <%= form.text_field :fa_id, id: :rating_fa_id %>
  </div> 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
